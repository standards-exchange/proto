syntax = "proto3";

package standards.open.v1;

import "google/type/date.proto";

option go_package = "github.com/standards-exchange/protobuf-go/standards/open/v1";

// CSSF report containing Identifiers of Luxembourg-based UCI/SIF/SICAR compartments or share classes
message RT000H944 {
  // The records provided in the report
  repeated Record records = 1;
  // Definition of an individual record in the report
  message Record {
    // Metadata
    string e = 1;
    // The CSSF's idenfifier for the umbrella
    string nnnnnnnn = 2;
    // The CSSF's idenfifier for the umbrella
    // NOMOPC
    string nomopc = 3;
    // The CSSF's identifier for the fund
    string cccccccc = 4;
    // The name of the sub-fund / compartment
    // NOMCOMPARTIMENT
    string nomcompaortiment = 5;
    // AGREEMENTCOMP
    google.type.Date agreementcomp = 6;
    // DEVISECOMP
    string devisecomp = 7;
    // PPPP
    string pppp = 8;
    // NOMTYPEPART
    string nomtypepart = 9;
  }
}